package scope

// filler keeps this test file exactly one line past the shipped default; 1
// filler keeps this test file exactly one line past the shipped default; 2
// filler keeps this test file exactly one line past the shipped default; 3
// filler keeps this test file exactly one line past the shipped default; 4
// filler keeps this test file exactly one line past the shipped default; 5
// filler keeps this test file exactly one line past the shipped default; 6
// filler keeps this test file exactly one line past the shipped default; 7
// filler keeps this test file exactly one line past the shipped default; 8
// filler keeps this test file exactly one line past the shipped default; 9
// filler keeps this test file exactly one line past the shipped default; 10
// filler keeps this test file exactly one line past the shipped default; 11
// filler keeps this test file exactly one line past the shipped default; 12
// filler keeps this test file exactly one line past the shipped default; 13
// filler keeps this test file exactly one line past the shipped default; 14
// filler keeps this test file exactly one line past the shipped default; 15
// filler keeps this test file exactly one line past the shipped default; 16
// filler keeps this test file exactly one line past the shipped default; 17
// filler keeps this test file exactly one line past the shipped default; 18
// filler keeps this test file exactly one line past the shipped default; 19
// filler keeps this test file exactly one line past the shipped default; 20
// filler keeps this test file exactly one line past the shipped default; 21
// filler keeps this test file exactly one line past the shipped default; 22
// filler keeps this test file exactly one line past the shipped default; 23
// filler keeps this test file exactly one line past the shipped default; 24
// filler keeps this test file exactly one line past the shipped default; 25
// filler keeps this test file exactly one line past the shipped default; 26
// filler keeps this test file exactly one line past the shipped default; 27
// filler keeps this test file exactly one line past the shipped default; 28
// filler keeps this test file exactly one line past the shipped default; 29
// filler keeps this test file exactly one line past the shipped default; 30
// filler keeps this test file exactly one line past the shipped default; 31
// filler keeps this test file exactly one line past the shipped default; 32
// filler keeps this test file exactly one line past the shipped default; 33
// filler keeps this test file exactly one line past the shipped default; 34
// filler keeps this test file exactly one line past the shipped default; 35
// filler keeps this test file exactly one line past the shipped default; 36
// filler keeps this test file exactly one line past the shipped default; 37
// filler keeps this test file exactly one line past the shipped default; 38
// filler keeps this test file exactly one line past the shipped default; 39
// filler keeps this test file exactly one line past the shipped default; 40
// filler keeps this test file exactly one line past the shipped default; 41
// filler keeps this test file exactly one line past the shipped default; 42
// filler keeps this test file exactly one line past the shipped default; 43
// filler keeps this test file exactly one line past the shipped default; 44
// filler keeps this test file exactly one line past the shipped default; 45
// filler keeps this test file exactly one line past the shipped default; 46
// filler keeps this test file exactly one line past the shipped default; 47
// filler keeps this test file exactly one line past the shipped default; 48
// filler keeps this test file exactly one line past the shipped default; 49
// filler keeps this test file exactly one line past the shipped default; 50
// filler keeps this test file exactly one line past the shipped default; 51
// filler keeps this test file exactly one line past the shipped default; 52
// filler keeps this test file exactly one line past the shipped default; 53
// filler keeps this test file exactly one line past the shipped default; 54
// filler keeps this test file exactly one line past the shipped default; 55
// filler keeps this test file exactly one line past the shipped default; 56
// filler keeps this test file exactly one line past the shipped default; 57
// filler keeps this test file exactly one line past the shipped default; 58
// filler keeps this test file exactly one line past the shipped default; 59
// filler keeps this test file exactly one line past the shipped default; 60
// filler keeps this test file exactly one line past the shipped default; 61
// filler keeps this test file exactly one line past the shipped default; 62
// filler keeps this test file exactly one line past the shipped default; 63
// filler keeps this test file exactly one line past the shipped default; 64
// filler keeps this test file exactly one line past the shipped default; 65
// filler keeps this test file exactly one line past the shipped default; 66
// filler keeps this test file exactly one line past the shipped default; 67
// filler keeps this test file exactly one line past the shipped default; 68
// filler keeps this test file exactly one line past the shipped default; 69
// filler keeps this test file exactly one line past the shipped default; 70
// filler keeps this test file exactly one line past the shipped default; 71
// filler keeps this test file exactly one line past the shipped default; 72
// filler keeps this test file exactly one line past the shipped default; 73
// filler keeps this test file exactly one line past the shipped default; 74
// filler keeps this test file exactly one line past the shipped default; 75
// filler keeps this test file exactly one line past the shipped default; 76
// filler keeps this test file exactly one line past the shipped default; 77
// filler keeps this test file exactly one line past the shipped default; 78
// filler keeps this test file exactly one line past the shipped default; 79
// filler keeps this test file exactly one line past the shipped default; 80
// filler keeps this test file exactly one line past the shipped default; 81
// filler keeps this test file exactly one line past the shipped default; 82
// filler keeps this test file exactly one line past the shipped default; 83
// filler keeps this test file exactly one line past the shipped default; 84
// filler keeps this test file exactly one line past the shipped default; 85
// filler keeps this test file exactly one line past the shipped default; 86
// filler keeps this test file exactly one line past the shipped default; 87
// filler keeps this test file exactly one line past the shipped default; 88
// filler keeps this test file exactly one line past the shipped default; 89
// filler keeps this test file exactly one line past the shipped default; 90
// filler keeps this test file exactly one line past the shipped default; 91
// filler keeps this test file exactly one line past the shipped default; 92
// filler keeps this test file exactly one line past the shipped default; 93
// filler keeps this test file exactly one line past the shipped default; 94
// filler keeps this test file exactly one line past the shipped default; 95
// filler keeps this test file exactly one line past the shipped default; 96
// filler keeps this test file exactly one line past the shipped default; 97
// filler keeps this test file exactly one line past the shipped default; 98
// filler keeps this test file exactly one line past the shipped default; 99
// filler keeps this test file exactly one line past the shipped default; 100
// filler keeps this test file exactly one line past the shipped default; 101
// filler keeps this test file exactly one line past the shipped default; 102
// filler keeps this test file exactly one line past the shipped default; 103
// filler keeps this test file exactly one line past the shipped default; 104
// filler keeps this test file exactly one line past the shipped default; 105
// filler keeps this test file exactly one line past the shipped default; 106
// filler keeps this test file exactly one line past the shipped default; 107
// filler keeps this test file exactly one line past the shipped default; 108
// filler keeps this test file exactly one line past the shipped default; 109
// filler keeps this test file exactly one line past the shipped default; 110
// filler keeps this test file exactly one line past the shipped default; 111
// filler keeps this test file exactly one line past the shipped default; 112
// filler keeps this test file exactly one line past the shipped default; 113
// filler keeps this test file exactly one line past the shipped default; 114
// filler keeps this test file exactly one line past the shipped default; 115
// filler keeps this test file exactly one line past the shipped default; 116
// filler keeps this test file exactly one line past the shipped default; 117
// filler keeps this test file exactly one line past the shipped default; 118
// filler keeps this test file exactly one line past the shipped default; 119
// filler keeps this test file exactly one line past the shipped default; 120
// filler keeps this test file exactly one line past the shipped default; 121
// filler keeps this test file exactly one line past the shipped default; 122
// filler keeps this test file exactly one line past the shipped default; 123
// filler keeps this test file exactly one line past the shipped default; 124
// filler keeps this test file exactly one line past the shipped default; 125
// filler keeps this test file exactly one line past the shipped default; 126
// filler keeps this test file exactly one line past the shipped default; 127
// filler keeps this test file exactly one line past the shipped default; 128
// filler keeps this test file exactly one line past the shipped default; 129
// filler keeps this test file exactly one line past the shipped default; 130
// filler keeps this test file exactly one line past the shipped default; 131
// filler keeps this test file exactly one line past the shipped default; 132
// filler keeps this test file exactly one line past the shipped default; 133
// filler keeps this test file exactly one line past the shipped default; 134
// filler keeps this test file exactly one line past the shipped default; 135
// filler keeps this test file exactly one line past the shipped default; 136
// filler keeps this test file exactly one line past the shipped default; 137
// filler keeps this test file exactly one line past the shipped default; 138
// filler keeps this test file exactly one line past the shipped default; 139
// filler keeps this test file exactly one line past the shipped default; 140
// filler keeps this test file exactly one line past the shipped default; 141
// filler keeps this test file exactly one line past the shipped default; 142
// filler keeps this test file exactly one line past the shipped default; 143
// filler keeps this test file exactly one line past the shipped default; 144
// filler keeps this test file exactly one line past the shipped default; 145
// filler keeps this test file exactly one line past the shipped default; 146
// filler keeps this test file exactly one line past the shipped default; 147
// filler keeps this test file exactly one line past the shipped default; 148
// filler keeps this test file exactly one line past the shipped default; 149
// filler keeps this test file exactly one line past the shipped default; 150
// filler keeps this test file exactly one line past the shipped default; 151
// filler keeps this test file exactly one line past the shipped default; 152
// filler keeps this test file exactly one line past the shipped default; 153
// filler keeps this test file exactly one line past the shipped default; 154
// filler keeps this test file exactly one line past the shipped default; 155
// filler keeps this test file exactly one line past the shipped default; 156
// filler keeps this test file exactly one line past the shipped default; 157
// filler keeps this test file exactly one line past the shipped default; 158
// filler keeps this test file exactly one line past the shipped default; 159
// filler keeps this test file exactly one line past the shipped default; 160
// filler keeps this test file exactly one line past the shipped default; 161
// filler keeps this test file exactly one line past the shipped default; 162
// filler keeps this test file exactly one line past the shipped default; 163
// filler keeps this test file exactly one line past the shipped default; 164
// filler keeps this test file exactly one line past the shipped default; 165
// filler keeps this test file exactly one line past the shipped default; 166
// filler keeps this test file exactly one line past the shipped default; 167
// filler keeps this test file exactly one line past the shipped default; 168
// filler keeps this test file exactly one line past the shipped default; 169
// filler keeps this test file exactly one line past the shipped default; 170
// filler keeps this test file exactly one line past the shipped default; 171
// filler keeps this test file exactly one line past the shipped default; 172
// filler keeps this test file exactly one line past the shipped default; 173
// filler keeps this test file exactly one line past the shipped default; 174
// filler keeps this test file exactly one line past the shipped default; 175
// filler keeps this test file exactly one line past the shipped default; 176
// filler keeps this test file exactly one line past the shipped default; 177
// filler keeps this test file exactly one line past the shipped default; 178
// filler keeps this test file exactly one line past the shipped default; 179
// filler keeps this test file exactly one line past the shipped default; 180
// filler keeps this test file exactly one line past the shipped default; 181
// filler keeps this test file exactly one line past the shipped default; 182
// filler keeps this test file exactly one line past the shipped default; 183
// filler keeps this test file exactly one line past the shipped default; 184
// filler keeps this test file exactly one line past the shipped default; 185
// filler keeps this test file exactly one line past the shipped default; 186
// filler keeps this test file exactly one line past the shipped default; 187
// filler keeps this test file exactly one line past the shipped default; 188
// filler keeps this test file exactly one line past the shipped default; 189
// filler keeps this test file exactly one line past the shipped default; 190
// filler keeps this test file exactly one line past the shipped default; 191
// filler keeps this test file exactly one line past the shipped default; 192
// filler keeps this test file exactly one line past the shipped default; 193
// filler keeps this test file exactly one line past the shipped default; 194
// filler keeps this test file exactly one line past the shipped default; 195
// filler keeps this test file exactly one line past the shipped default; 196
// filler keeps this test file exactly one line past the shipped default; 197
// filler keeps this test file exactly one line past the shipped default; 198
// filler keeps this test file exactly one line past the shipped default; 199
// filler keeps this test file exactly one line past the shipped default; 200
// filler keeps this test file exactly one line past the shipped default; 201
// filler keeps this test file exactly one line past the shipped default; 202
// filler keeps this test file exactly one line past the shipped default; 203
// filler keeps this test file exactly one line past the shipped default; 204
// filler keeps this test file exactly one line past the shipped default; 205
// filler keeps this test file exactly one line past the shipped default; 206
// filler keeps this test file exactly one line past the shipped default; 207
// filler keeps this test file exactly one line past the shipped default; 208
// filler keeps this test file exactly one line past the shipped default; 209
// filler keeps this test file exactly one line past the shipped default; 210
// filler keeps this test file exactly one line past the shipped default; 211
// filler keeps this test file exactly one line past the shipped default; 212
// filler keeps this test file exactly one line past the shipped default; 213
// filler keeps this test file exactly one line past the shipped default; 214
// filler keeps this test file exactly one line past the shipped default; 215
// filler keeps this test file exactly one line past the shipped default; 216
// filler keeps this test file exactly one line past the shipped default; 217
// filler keeps this test file exactly one line past the shipped default; 218
// filler keeps this test file exactly one line past the shipped default; 219
// filler keeps this test file exactly one line past the shipped default; 220
// filler keeps this test file exactly one line past the shipped default; 221
// filler keeps this test file exactly one line past the shipped default; 222
// filler keeps this test file exactly one line past the shipped default; 223
// filler keeps this test file exactly one line past the shipped default; 224
// filler keeps this test file exactly one line past the shipped default; 225
// filler keeps this test file exactly one line past the shipped default; 226
// filler keeps this test file exactly one line past the shipped default; 227
// filler keeps this test file exactly one line past the shipped default; 228
// filler keeps this test file exactly one line past the shipped default; 229
// filler keeps this test file exactly one line past the shipped default; 230
// filler keeps this test file exactly one line past the shipped default; 231
// filler keeps this test file exactly one line past the shipped default; 232
// filler keeps this test file exactly one line past the shipped default; 233
// filler keeps this test file exactly one line past the shipped default; 234
// filler keeps this test file exactly one line past the shipped default; 235
// filler keeps this test file exactly one line past the shipped default; 236
// filler keeps this test file exactly one line past the shipped default; 237
// filler keeps this test file exactly one line past the shipped default; 238
// filler keeps this test file exactly one line past the shipped default; 239
// filler keeps this test file exactly one line past the shipped default; 240
// filler keeps this test file exactly one line past the shipped default; 241
// filler keeps this test file exactly one line past the shipped default; 242
// filler keeps this test file exactly one line past the shipped default; 243
// filler keeps this test file exactly one line past the shipped default; 244
// filler keeps this test file exactly one line past the shipped default; 245
// filler keeps this test file exactly one line past the shipped default; 246
// filler keeps this test file exactly one line past the shipped default; 247
// filler keeps this test file exactly one line past the shipped default; 248
// filler keeps this test file exactly one line past the shipped default; 249
// filler keeps this test file exactly one line past the shipped default; 250
// filler keeps this test file exactly one line past the shipped default; 251
// filler keeps this test file exactly one line past the shipped default; 252
// filler keeps this test file exactly one line past the shipped default; 253
// filler keeps this test file exactly one line past the shipped default; 254
// filler keeps this test file exactly one line past the shipped default; 255
// filler keeps this test file exactly one line past the shipped default; 256
// filler keeps this test file exactly one line past the shipped default; 257
// filler keeps this test file exactly one line past the shipped default; 258
// filler keeps this test file exactly one line past the shipped default; 259
// filler keeps this test file exactly one line past the shipped default; 260
// filler keeps this test file exactly one line past the shipped default; 261
// filler keeps this test file exactly one line past the shipped default; 262
// filler keeps this test file exactly one line past the shipped default; 263
// filler keeps this test file exactly one line past the shipped default; 264
// filler keeps this test file exactly one line past the shipped default; 265
// filler keeps this test file exactly one line past the shipped default; 266
// filler keeps this test file exactly one line past the shipped default; 267
// filler keeps this test file exactly one line past the shipped default; 268
// filler keeps this test file exactly one line past the shipped default; 269
// filler keeps this test file exactly one line past the shipped default; 270
// filler keeps this test file exactly one line past the shipped default; 271
// filler keeps this test file exactly one line past the shipped default; 272
// filler keeps this test file exactly one line past the shipped default; 273
// filler keeps this test file exactly one line past the shipped default; 274
// filler keeps this test file exactly one line past the shipped default; 275
// filler keeps this test file exactly one line past the shipped default; 276
// filler keeps this test file exactly one line past the shipped default; 277
// filler keeps this test file exactly one line past the shipped default; 278
// filler keeps this test file exactly one line past the shipped default; 279
// filler keeps this test file exactly one line past the shipped default; 280
// filler keeps this test file exactly one line past the shipped default; 281
// filler keeps this test file exactly one line past the shipped default; 282
// filler keeps this test file exactly one line past the shipped default; 283
// filler keeps this test file exactly one line past the shipped default; 284
// filler keeps this test file exactly one line past the shipped default; 285
// filler keeps this test file exactly one line past the shipped default; 286
// filler keeps this test file exactly one line past the shipped default; 287
// filler keeps this test file exactly one line past the shipped default; 288
// filler keeps this test file exactly one line past the shipped default; 289
// filler keeps this test file exactly one line past the shipped default; 290
// filler keeps this test file exactly one line past the shipped default; 291
// filler keeps this test file exactly one line past the shipped default; 292
// filler keeps this test file exactly one line past the shipped default; 293
// filler keeps this test file exactly one line past the shipped default; 294
// filler keeps this test file exactly one line past the shipped default; 295

// Helper is the out-of-scope side of the pair: byte-for-byte the same length as
// scope.go, differing only in the _test.go suffix, and never reported.
func Helper() int { return 2 }
